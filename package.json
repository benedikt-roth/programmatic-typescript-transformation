{
  "name": "ts-to-ts-transform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretransform": "if [ -d out ]; then rm -R out; fi && mkdir out && cp -R dummy-files/** out",
    "transform": "./node_modules/.bin/ts-node src/transform.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-morph": "^3.1.3",
    "ts-node": "^8.3.0"
  }
}
